<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hp.TexasPoker.mapper.PlayerMapper">
    <insert id="createNewPlayer">
        insert into hp_player_info(player_id, player_name, password, score, rank)
        values (#{playerId}, #{playerName}, #{password}, #{score}, #{rank})
    </insert>
</mapper>